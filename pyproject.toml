[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "oreon"
version = "0.1.0"
description = "Oreon AI Assistant"
requires-python = ">=3.10"
dependencies = [
    "tabulate>=0.9.0"
]
[project.scripts]
oreon = "oreon.cli:main"